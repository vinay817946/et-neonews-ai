import { Captions, Clapperboard, Layers3 } from "lucide-react";
import GlassCard from "@/components/ui/GlassCard";
function normalizeScenes(scenes = []) { return scenes.map((scene, index) => { if (typeof scene === "string") { return { title: `Scene ${index + 1}`, narration: scene, visual: "Animated headline + stock visual treatment", overlay: "Key metric / callout caption", duration: `${15 + index * 3}s` }; } return { title: scene.title || `Scene ${index + 1}`, narration: scene.narration || scene.copy || scene.text || "Narration placeholder", visual: scene.visual || "Data visual + motion background", overlay: scene.overlay || "Headline overlay", duration: scene.duration || `${15 + index * 3}s` }; }); }
export default function ScriptScenes({ scenes }) { const normalizedScenes = normalizeScenes(scenes); return <GlassCard className="neo-panel-strong rounded-[32px] p-6"><div className="flex items-center justify-between gap-3 flex-wrap"><div><p className="text-sm uppercase tracking-[0.2em] text-cyan-300">Video flow</p><h3 className="mt-1 text-2xl font-semibold tracking-tight">Generated scene board</h3></div><span className="pill">{normalizedScenes.length} scenes</span></div><div className="mt-5 space-y-4">{normalizedScenes.map((scene, index) => <div key={`${scene.title}-${index}`} className="rounded-[28px] border border-white/10 bg-slate-950/60 p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div className="inline-flex items-center gap-3"><span className="inline-flex h-10 w-10 items-center justify-center rounded-2xl border border-cyan-300/16 bg-cyan-400/10 text-sm font-semibold text-cyan-200">0{index + 1}</span><div><p className="font-semibold">{scene.title}</p><p className="text-sm text-white/48">{scene.duration}</p></div></div><span className="pill">{index === 0 ? "Hook" : index === normalizedScenes.length - 1 ? "Outro" : "Build"}</span></div><div className="mt-4 grid gap-3 md:grid-cols-3"><div className="rounded-2xl border border-white/10 bg-white/5 p-4 text-sm text-white/70"><div className="mb-2 flex items-center gap-2 text-cyan-200"><Clapperboard size={15} /> Visual</div>{scene.visual}</div><div className="rounded-2xl border border-white/10 bg-white/5 p-4 text-sm text-white/70"><div className="mb-2 flex items-center gap-2 text-cyan-200"><Captions size={15} /> Narration</div>{scene.narration}</div><div className="rounded-2xl border border-white/10 bg-white/5 p-4 text-sm text-white/70"><div className="mb-2 flex items-center gap-2 text-cyan-200"><Layers3 size={15} /> Overlay</div>{scene.overlay}</div></div></div>)}</div></GlassCard>; }
